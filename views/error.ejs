<%- include('partials/header') %>

<div class="container text-center py-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <i class="fas fa-exclamation-triangle fa-5x text-warning mb-4"></i>
            <h1 class="display-1 fw-bold"><%= locals.statusCode || 404 %></h1>
            <h2 class="mb-4"><%= locals.message || 'Page Not Found' %></h2>
            <p class="text-muted mb-4">Sorry, we couldn't find the page you're looking for.</p>
            <a href="/" class="btn btn-warning btn-lg">Go Back Home</a>
        </div>
    </div>
</div>

<%- include('partials/footer') %>